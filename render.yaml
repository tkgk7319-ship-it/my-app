services:
  - type: web
    name: receipt-manager
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        sync: false
      - key: NODE_ENV
        value: production
